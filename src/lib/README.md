# src/lib

Domain library code goes here per plan.md section 9 (e.g., `wp-client.ts`, `sanitize.ts`, `sync.ts`, `graphql-fetch.ts`, `cache-tags.ts`, `seo.ts`).

The root-level `lib/utils.ts` is a shadcn/ui artifact (the `cn()` helper) and is referenced by `@/lib/utils`. Do not move it.
