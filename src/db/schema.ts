// Placeholder: real Drizzle schema lands in Wave 2A (plan.md section 5).
// drizzle-kit refuses to start studio when this path is missing, so we keep
// the file present and intentionally empty. Do not add tables here, the next
// agent owns this file.
export {};
